#!/usr/bin/python
# -*- coding: utf-8 -*-

from distutils.core import setup

setup(name="scservices", version="@VERSION@",
        packages=["scservices", "scservices.core",
            "scservices.core.systemd", "scservices.core.systemd.constants",
            "scservices.core.legacy", "scservices.gui"])

setup(name="scservices.dbus", version="@VERSION@",
        packages=["scservices.dbus" , "scservices.dbus.service",
            "scservices.dbus.service.systemd", "scservices.dbus.proxy"])
