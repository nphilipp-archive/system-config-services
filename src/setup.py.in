#!/usr/bin/python
# -*- coding: utf-8 -*-

from distutils.core import setup

setup(name="scservices", version="@VERSION@", packages=["scservices",
      "scservices.core"])

setup(name="scservices.dbus", version="@VERSION@",
      packages=["scservices.dbus" , "scservices.dbus.service",
                "scservices.dbus.proxy"])
